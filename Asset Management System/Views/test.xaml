<catel:DataWindow x:Class="Asset_Management_System.Views.test"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:catel="http://catel.codeplex.com">

    <catel:StackGrid>
        <catel:StackGrid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </catel:StackGrid.RowDefinitions>

        <DataGrid ItemsSource="{Binding ItemCollection,UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding Item,UpdateSourceTrigger=PropertyChanged}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Brand" Binding="{Binding ItemBrandName,UpdateSourceTrigger=PropertyChanged}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Border BorderThickness="3" BorderBrush="Blue">
        <TextBlock Text="{Binding Path=ItemBrandName,UpdateSourceTrigger=PropertyChanged}"/>
        </Border>

        <Button Command="{Binding additem}" Content="Add" Width="50" HorizontalAlignment="Left"/>
        
    </catel:StackGrid>
    
</catel:DataWindow>
